---
export interface Props {
  class?: string;
  size?: "sm" | "lg";
  readingTimeEstimate: string;
}

const { readingTimeEstimate, size = "sm", class: className = "" } = Astro.props;
---

<div class:list={["flex items-center gap-x-2 opacity-80", className]}>
  <p>~ {readingTimeEstimate}</p>
</div>
